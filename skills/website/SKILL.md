---
name: website
description: Use when building, designing, or editing a website, landing page, or web app on the DeployNow stack — Next.js App Router, React, Tailwind + DaisyUI, NextAuth, deployed on Vercel. Start here for visual and design work too — this is the design stack: it routes the design skills (brief and message, taste read, design system, build, polish, review) in working order, so invoke it before any of them. Trigger phrases: "design stack", "usa el design stack", "diseña un sitio / una landing / una página". Use when scaffolding a page/component/feature, choosing a visual direction, palette or typography, naming files or components, deciding server vs client components, fixing a broken or cramped layout, optimizing images or Web Vitals, handling async errors, or deploying. Not for non-web code, native/mobile apps, or generic React unrelated to this stack.
---

# website

Build production-grade, distinctive Next.js websites the DeployNow way — server-first, layout-stable, deployed on Vercel.

> **🌐 ACTIVE-SKILL MARKER:** Prefix your reply with 🌐 **only on turns where the work touches the `website` domain** — the Next.js + Tailwind/DeployNow web stack — regardless of the layer/project (frontend, backend, a local script — all count); what matters is whether *this turn* touches the domain. On turns that do NOT touch it (typecheck, build, deploy, git ops, editing or curl in other domains), **omit 🌐** even if the skill loaded earlier in the session. If other active skills also apply to the same turn, **stack their emojis** in the prefix.

## Overview

The stack is **DeployNow**: JavaScript, Node.js, React, Next.js App Router, Tailwind CSS + DaisyUI, NextAuth, deployed on Vercel. Two principles dominate everything else:

- **Server-first.** Default to React Server Components and SSR. `'use client'`, `useState`, `useEffect` are exceptions you justify, not defaults you reach for. See [reference/server-first.md](reference/server-first.md).
- **Layout stability.** Dynamic content must never displace its siblings, and a broken layout is almost always an *ancestor* constraint — fix the parent, never patch the leaf. See [reference/layout.md](reference/layout.md).

Design is **production-grade and distinctive** — never cookie-cutter. Mobile-first, responsive, ready to ship. The design *method* is not in this file: it lives in dedicated skills, and the [Design stack](#design-stack--the-order-of-work-for-anything-visual) below says which one to invoke at each phase. Do not design from memory.

## When to use

- Building a page, component, or feature for a website / web app.
- Naming files or components, or laying out the file structure.
- Deciding server vs client components, data fetching, Suspense.
- Fixing a broken, cramped, or misaligned layout.
- Optimizing images or Web Vitals; handling async errors; deploying to Vercel.

**Not for:** non-web code, native/mobile apps, or generic React questions unrelated to this stack.

## Design stack — the order of work for anything visual

This skill owns the *stack* rules. Design method lives in dedicated skills, and a skill only helps if it is **invoked** — an installed design skill that is never called produces the same generic page as no skill at all (measured on real client sites: the first pass came out cream + serif + gold, exactly the look the taste skill forbids, because nobody had loaded it). So for any task with a visual outcome, walk this table top to bottom and invoke each phase's skill **before** doing that phase's work.

**Full build** — a new site, a new landing page, a section with its own identity, or a redesign — every phase, in this order:

| # | Phase | What gets decided | Invoke |
|---|---|---|---|
| 0 | Brief & message | Who it is for, the ONE action the page exists for, the message hierarchy (one-liner, stakes, plan) | `superpowers:brainstorming` → `marketing-cro:storybrand-messaging` → `marketing-cro:cro-methodology` |
| 1 | Design read | The direction the brief implies — variance / motion / density dials — and the defaults you are explicitly *not* taking | `design-taste-frontend` (§0–1: brief inference, the three dials, anti-default discipline) |
| 2 | Design system | Style, palette, type pairing, spacing / radius / shadow scales — one token system, not per-section improvisation | `ui-ux-pro-max:ui-ux-pro-max` (search its local DB of styles, palettes, pairings) → `ux-design:web-typography` → `ux-design:refactoring-ui` |
| 3 | Build | Structure, sections, layout, RSC/client split | `frontend-design:frontend-design` (brainstorm → explore → plan → critique → build → critique again) + this skill's stack rules |
| 4 | Polish | Hover / focus / active states, motion restraint, borders, shadows, optical alignment, tabular numbers | `make-interfaces-feel-better` → `ux-design:microinteractions` |
| 5 | Assets | Hero video, product imagery, charts, banners — only when the page needs them | `hyperframes` (video), `media-use` (BGM / SFX / images), `luna-skills:luna` (CDN), `dataviz` (charts), `ui-ux-pro-max:banner-design` |
| 6 | Review | AI-tells pre-flight, usability audit or taste review, visual QA at desktop width | `design-taste-frontend` (§9 forbidden patterns) → `ux-design:ux-heuristics` or `ux-design:steve-jobs-design-review` → `pixelbrowse:pixelbrowse` at 1440 px, light and dark → `superpowers:verification-before-completion` |
| 7 | Ship | Deploy, indexing, the analytics switch | this skill: [reference/deploy.md](reference/deploy.md), [reference/deploy-verification-witness-string.md](reference/deploy-verification-witness-string.md), [reference/indexing-and-search-console.md](reference/indexing-and-search-console.md), [reference/vercel-web-analytics.md](reference/vercel-web-analytics.md) |

**Partial tasks** — route by the shape of the request, not by habit:

- A new component or section inside an existing design → phases 3 → 4 → 6 (pre-flight only).
- "It looks generic / AI-made / low-level", with or without a reference → capture yours and the reference at the **same** width (1440, not the 875 default) and work from the diff, never from taste alone; then phase 1 → 6 → 4.
- Copy only → phase 0 (`marketing-cro:storybrand-messaging`).
- Layout broken / cramped / misaligned → [reference/layout.md](reference/layout.md) only — no design skills.
- An app or panel (POS, admin, dashboard) rather than a landing → phases 1–4 + 6, plus `ux-design:design-everyday-things` (affordances); add `ux-design:hooked-ux` / `ux-design:improve-retention` only when habit or retention is the goal. Skip phase 0's CRO.
- A showcase piece where the page itself is the product (portfolio, agency hero, "Awwwards quality") → add `ux-design:top-design` between phases 2 and 3.
- The whole build guided phase by phase, with a question at every decision and living docs in the repo's `docs/` → `metaskills:create-website` (a journey; it writes into `docs/`).

The design plugins (`ux-design`, `ui-ux-pro-max`, `marketing-cro`, `metaskills`) are enabled **per repo**, not globally — `claude plugin enable <plugin> --scope project`, effective next session. If a `Skill` call above fails with an unknown skill, the repo has not been enabled: say so and enable it; do not silently skip the phase.

## Where things live

| Task | Reference |
|---|---|
| Stack, file structure, file/component naming, pure functions, JSX unicode gotcha | [reference/stack-and-conventions.md](reference/stack-and-conventions.md) |
| Server vs client, RSC/SSR, `'use client'` rules, `swr`, Suspense, code skeletons | [reference/server-first.md](reference/server-first.md) |
| Layout stability + the trace-upward debugging method; why `width:fit-content` on a flex row collapses instead of filling, and the `--n` count + `min()`/`calc()` fix; a flex-column child silently collapsing to zero height in a fixed-height container (print/PDF) and the `flex-shrink: 0` fix | [reference/layout.md](reference/layout.md) |
| WebP images, lazy loading, Web Vitals (LCP/CLS/FID) | [reference/performance.md](reference/performance.md) |
| Static vs dynamic rendering: how one uncached fetch in the layout turns every route dynamic, reading the build's route table, choosing a cache policy per read | [reference/static-vs-dynamic-rendering.md](reference/static-vs-dynamic-rendering.md) |
| Vercel deploy, error handling, external-CDN / no double-hosting; why a gitignored route still ships in `vercel deploy` and only `.vercelignore` stops it, the take-down procedure and its objective control; and the second, invisible double-hosting door — `next/image` re-hosting a CDN image on Vercel's own domain, breaking only NEW uploads with a `402` | [reference/deploy.md](reference/deploy.md) |
| Checking a repo with no `node_modules`: bun's transpiler as a JSX syntax gate before pushing, running logic tests that need the `@/` alias, and the shell traps that make a gate lie about its own exit code | [reference/syntax-gate-without-node-modules.md](reference/syntax-gate-without-node-modules.md) |
| Third-party scripts (pixels/analytics/widgets) under a strict CSP — silent dead-pixel trap + post-deploy verification | [reference/third-party-scripts-csp.md](reference/third-party-scripts-csp.md) |
| Getting indexed: `robots.js`/`sitemap.js` derived from the catalogue, what a sitemap must NOT carry, why `robots.txt` blocks crawling but not indexing, the canonical host constant + `alternates.canonical`, apex vs `www` both answering, Search Console steps for the operator | [reference/indexing-and-search-console.md](reference/indexing-and-search-console.md) |
| Vercel Web Analytics: the per-project switch that `<Analytics />` does not flip, diagnosing it, free-tier event limits | [reference/vercel-web-analytics.md](reference/vercel-web-analytics.md) |
| Local business: auditing the Google Business Profile listing with the Places API, why declared hours gate the local block, owner vs manager access | [reference/google-business-profile.md](reference/google-business-profile.md) |
| Brand searches: getting the brand string into `<title>`/`<h1>`, the three distinct name constants, title character budget | [reference/brand-in-title.md](reference/brand-in-title.md) |
| Mobile-first: tab rows → scroll horizontal, grid shrink, tap targets, density | [reference/mobile-design.md](reference/mobile-design.md) |
| Client logo row from favicons: baked color plates vs transparent, round-badge normalization, wrap/centering, `alt` and the confidentiality question | [reference/client-logo-row.md](reference/client-logo-row.md) |
| Page-wide watermark: why a layer behind opaque sections disappears, the `mix-blend-multiply` overlay, z-index budget, measuring the opacity against WCAG contrast | [reference/page-watermark.md](reference/page-watermark.md) |
| Hero/catalogue imagery from real product photos: generating the room around the product instead of pasting a cut-out, why the light of the source photo (not the perspective) breaks the composite, contact-shadow dead ends, the go/no-go arbiter | [reference/product-photo-compositing.md](reference/product-photo-compositing.md) |
| PNG→SVG line-art tracing: why the diff-ratio metric is noise, the ring/cores referee, potrace flags, baking the inverted-Y transform, one `<defs>` + `<use>` | [reference/svg-line-art-tracing.md](reference/svg-line-art-tracing.md) |
| API boundary: sending identity fields explicitly, NFC vs NFD exact-match failures, typing the response you measured instead of the one that was documented, a field that changes *meaning* and inverts every UI verdict (boolean vs status field, `""` as absence of instrument, re-keying the cache signature, routing on a body `code` instead of the HTTP status) | [reference/api-boundary-contracts.md](reference/api-boundary-contracts.md) |
| Dates: a Postgres `date` arriving as midnight UTC and shifting every row a day, the noon-UTC anchor, calendar days vs instants as separate types, a timestamp without the trailing `Z` parsed as local time, why a bench running in UTC goes green with the bug alive, ingesting an external export whose split date/time columns destroyed the offset | [reference/calendar-days-vs-instants.md](reference/calendar-days-vs-instants.md) |
| Deadlines in the UI: why the device clock may only decide *when to ask* and never whether a window expired, the `open` / `due` split, the one-refetch guard that stops the infinite loop, asking the endpoint that doesn't write, and the clock-skew assertions | [reference/client-clock-vs-server-deadline.md](reference/client-clock-vs-server-deadline.md) |
| Money screens: never mixing currencies, summing in integer cents, committed vs collected, showing gaps, grouper vs entity, captions read as assertions, a shared-visibility dashboard's output isolation (ship the signal, not the evidence), and why an exception covering the heaviest case means the rule is wrong | [reference/money-dashboard.md](reference/money-dashboard.md) |
| Phone numbers as identity keys: repairing a corrupt country prefix from the area code, validating the rule against duplicates already in the data, normalizing to E.164 at ingest, and why two unique-counts that agree can both be wrong | [reference/phone-normalization-and-dedup.md](reference/phone-normalization-and-dedup.md) |
| Domain email auth (SPF/DKIM): why grepping your own log for a success string is a guaranteed false negative, verifying with an external receiver instead, and the ENOENT noise that is not the defect | [reference/domain-email-auth.md](reference/domain-email-auth.md) |
| Draggable card strips: why `setPointerCapture` on `pointerdown` steals the click from child `<a>`s, the real-click probe, capturing on threshold instead, and why growing the image inside a butted card is invisible | [reference/draggable-card-strip.md](reference/draggable-card-strip.md) |
| CSS cascade layers: why an unlayered class in `globals.css` beats every Tailwind utility, and why it fails *halfway* (one property wins, another loses) | [reference/css-cascade-layers-vs-tailwind.md](reference/css-cascade-layers-vs-tailwind.md) |
| A shared class constant carrying `w-full`: why a per-instance `w-28` loses to stylesheet order, the `getBoundingClientRect` probe that proves the control is off screen, and the wrapping-DIV fix instead of `!important` | [reference/shared-class-constant-vs-per-instance-utility.md](reference/shared-class-constant-vs-per-instance-utility.md) |
| Opacity modifiers off Tailwind's step-of-5 scale (`bg-brand/12`): why no class is generated and the element paints fully transparent with no warning, the `<alpha-value>` prerequisite, the bracket syntax escape hatch, and the computed-color probe with its positive control | [reference/tailwind-opacity-modifier-scale.md](reference/tailwind-opacity-modifier-scale.md) |
| Decorative light sweeps: the resting surface a shine needs, `isolation` + `z-index:-1` so it doesn't erase text, why lowering opacity doesn't fix contrast, and how to measure an animation that moves | [reference/decorative-shine-sweep.md](reference/decorative-shine-sweep.md) |
| Linting a repo that already has findings: measuring the delta against `HEAD` from a worktree at the same relative path, why a baseline linted from `/tmp` silently runs fewer rules, comparing lines instead of counts | [reference/lint-delta-baseline.md](reference/lint-delta-baseline.md) |
| An overflowing flex row widening the whole document: measuring `scrollWidth` vs `clientWidth` headless instead of screenshotting, the scroll-container fix, why `shrink-0` is mandatory, mouse-only drag, one-sided edge fades, scrolling the active item into view | [reference/overflowing-row-scroll-container.md](reference/overflowing-row-scroll-container.md) |
| Surface tokens: why a `disabled:bg-<surface>` control vanishes at 1.00:1 on the screen that uses that surface as its page, the dedicated disabled fill + label tokens and their contrast targets, and why a mock harness can contradict the user's screenshot | [reference/surface-tokens-and-disabled-state.md](reference/surface-tokens-and-disabled-state.md) |
| PocketBase backends: why a `number` field cannot express "unknown" (`NOT NULL DEFAULT 0`), `select` vs a visibility hook, `0` swallowed by a falsy check in an `onRecordCreate` default, and why the signal for a denied rule (`200 []` / `404` / `403`) has to be measured per collection | [reference/pocketbase-schema-and-rules.md](reference/pocketbase-schema-and-rules.md) |
| PocketBase public forms that must stay re-editable: why locating the row with `filter=` needs `list` open, the deterministic explicit `id` that never lists or reads, the 403/404 probe with a made-up-collection control, and saying on screen when the backend did *not* save | [reference/pocketbase-public-editable-form.md](reference/pocketbase-public-editable-form.md) |
| User-facing copy: the three triggers that mark a string as team-facing (names a person, database vocabulary, assigns work to the reader / describes a role the reader IS), why "staff-only" is still client-facing when the owner has a staff account, implementing the promise a message makes, not inventing "all saved" on first paint, and why a `switch` on the error `code` alone collapses N distinct server reasons into one toast | [reference/user-facing-error-copy.md](reference/user-facing-error-copy.md) |
| Shipping the front ahead of its backend: the capability gate derived from the *response* (probing key **presence**, never `.length`, and before flattening), why the expensive failure is a control that lies about what it saved, saying on screen what is missing, and telling 401 from 404 on a route that isn't deployed yet | [reference/capability-gate-front-ahead-of-backend.md](reference/capability-gate-front-ahead-of-backend.md) |
| Focus rings inside `overflow-hidden rounded-*`: why the ring loses its four corners, why matching the child's radius makes it vanish entirely, the `has-[:focus-visible]` fix on the container, Tailwind v4 `outline-none` vs `outline-hidden` | [reference/focus-ring-clipped-by-rounded-parent.md](reference/focus-ring-clipped-by-rounded-parent.md) |
| Scroll-driven reveals (`animation-timeline: view()`, `IntersectionObserver`): why an element that never reaches its range stays at opacity 0 forever, why a `fullPage` screenshot resizes the viewport and hides exactly that, the computed-opacity probe in a real viewport and its mandatory `CSS.supports` positive control | [reference/scroll-reveal-stuck-at-opacity-zero.md](reference/scroll-reveal-stuck-at-opacity-zero.md) |
| Counting strings in a served page with `curl` + `grep`: why the RSC flight payload doubles every visible string, `grep -c` counting lines instead of occurrences, anchoring to `>text<` as the mechanism that discards the payload copy, React's `<!-- -->` separator truncating `[^<]*` so an interpolated value reads as blank, and the positive control that makes a zero mean something | [reference/counting-strings-in-rendered-html.md](reference/counting-strings-in-rendered-html.md) |
| Counting rows/comments from a paginated API (REST or Graph): why a single-page `limit=N` request returns a plausible, truncated total with no error, the cheap check (count == limit asked?), the positive control (rerun with a different limit), following Graph's `paging.next` as a complete URL, and the two-level trap of nested pagination | [reference/paginated-count-truncated-silently.md](reference/paginated-count-truncated-silently.md) |
| A vocabulary migration (invented catalogue → the client's real one): why the old code keeps working and a count over the retired universe reads as "nothing to do," classifying every use as read/count/write, the shared-attribute bridge instead of an `\|\|` of two lookups, and the mandatory seed-one positive control before trusting the new count | [reference/retired-vocabulary-vs-live-universe.md](reference/retired-vocabulary-vs-live-universe.md) |
| Verifying a deploy shipped YOUR commit: why polling a URL until it answers only proves liveness, why the witness string must be unique to the diff (not a class other components already use), why an accented witness is a guaranteed false negative in a minified bundle, and the positive control that closes the method | [reference/deploy-verification-witness-string.md](reference/deploy-verification-witness-string.md) |
| A module that is created but never imported: why it passes every code review, the one-line importer grep with a positive control, and the family of defects where the failure state and the success state produce the same observation | [reference/unwired-module-created-not-imported.md](reference/unwired-module-created-not-imported.md) |
| A capture-screen fix that only touches the READ side: why it looks perfect in the diff and answers "that object doesn't exist" on Save, the 6-step positive control that requires actually typing and saving data, and the sibling defect — a correct save that never reaches the screen because a `useSyncExternalStore` subscription (or a `useMemo` dependency) is missing | [reference/capture-half-fix-reads-not-writes.md](reference/capture-half-fix-reads-not-writes.md) |
| Piping an API response into `jq` from zsh: why `echo` turns a valid `\n` escape into a real newline and invalidates the JSON, why it only bites once the data has multi-line values, how an emptied variable turns into a misread `404`, and the `printf '%s'` / `<<<` / `-o file` forms that never corrupt it | [reference/shell-echo-corrupts-json.md](reference/shell-echo-corrupts-json.md) |
| A categorical palette reused as text color: why the contrast floor follows the color's **role** (mark 3.0 vs small text 4.5), measuring against the darkest surface the color can land on rather than the page, darkening by multiplying the channel so the hue survives, and storing the slug instead of the hex | [reference/categorical-palette-as-text-color.md](reference/categorical-palette-as-text-color.md) |
| Permissions and capabilities: the view/permission split that leaves an act reachable by nobody, capabilities granted before anything consults them, the four reachability properties a gate must assert instead of restating the role table, discarding translation maps by shape, probing a route's existence with 401 vs 404 plus a negative control, and asking "who can actually reach this" by logging in as that person whenever a flow is unblocked | [reference/permission-without-a-screen.md](reference/permission-without-a-screen.md) |
| Idempotent assignment routes (attach an existing file/tag/member to something): why a retry must never 422, `already_had_it` as an explicit field instead of the HTTP status (200/201 gets flattened by proxies), the assignment row's own id vs the file's uuid, "assign" adds instead of moves, and writing to the server before any local record | [reference/idempotent-assignment-endpoint.md](reference/idempotent-assignment-endpoint.md) |
| Mirroring a server limit in the client: counting code points instead of UTF-16 units or bytes (and replicating `trim` → NFC → count in order), why the server must deploy first, why a test that types the number by hand isn't a gate, and the positive marker that proves the deployed build when the change is just an integer | [reference/client-mirrored-server-limit.md](reference/client-mirrored-server-limit.md) |
| UI copy that asserts a fact about the client's business (not just about the code): why an inference needs the client's confirmation before it ships, wording a scope warning that stays true under any explanation, and why it belongs next to the number instead of in a footnote | [reference/client-business-claims-in-copy.md](reference/client-business-claims-in-copy.md) |
| A documented guarantee (a dedup rule, a validation) that lives in ONE writer's code, not in the table: why a second route touching the same resource doesn't inherit it, naming the writer explicitly in the doc, and checking the handler you actually call instead of trusting the README | [reference/guarantee-scoped-to-the-writer.md](reference/guarantee-scoped-to-the-writer.md) |
| A "set of labels" API field (`tags`, multi-select) that REPLACES the whole array on write instead of appending: canarying one row and reading it back before a batch, checking for automations that a burst of writes can trip, and verifying by re-deriving from source instead of the cached payload | [reference/collection-field-replace-vs-append.md](reference/collection-field-replace-vs-append.md) |
| A test fixture built around a hardcoded constant (chosen because it doesn't divide evenly, or falls inside a range): why it turns illegal or stops exercising its edge the day a business rule around it changes, deriving the value from the rule instead of typing it, and asserting the fixture's own premise so a false green can't hide | [reference/stale-test-fixture-constant.md](reference/stale-test-fixture-constant.md) |
| A test suite that calls a REPLACE (not upsert) sync endpoint against production: why exercising the endpoint exactly as documented is destructive with no bug required, why a self-healing defect that repopulates on the usual path stays invisible for months, giving destructive suites their own database, and why a success log for a later process is not evidence about the table's current state | [reference/test-suite-destructive-endpoint.md](reference/test-suite-destructive-endpoint.md) |
| A bugs/blockers file entry whose saved root cause is more than a day old: why it is a dated observation and not a fact in a multi-session repo, and re-measuring before working a bug instead of trusting what it says | [reference/stale-bug-diagnosis.md](reference/stale-bug-diagnosis.md) |
| Admin-entered names rendered elsewhere in the UI: ordering the distinguishing word first so `truncate` doesn't hide it, rewriting a templated sentence instead of guessing grammatical gender/number, the substring-classifier vs exact-match-lookup split that a rename silently breaks, and verifying with a positive control | [reference/admin-entered-names-in-ui.md](reference/admin-entered-names-in-ui.md) |
| Empirical-rule verification traps: why N matching rows can mean far fewer than N distinct observations, why measuring against a fixture your own code generated is circular, running a seeder/verifier twice unchanged as a check on itself, and correcting the evidence even when the conclusion doesn't change | [reference/empirical-rule-verification-traps.md](reference/empirical-rule-verification-traps.md) |
| Why a weighted average of ratios prints and quotes exactly like a measured rate, the one-line check (does any row actually show this number?), and the same defect one layer up — a count measuring one table while the conclusion speaks about the whole system | [reference/average-of-rates-is-not-a-rate.md](reference/average-of-rates-is-not-a-rate.md) |
| Why a blocker relayed by a peer agent is a claim, not a fact, until checked against your own state — and why hiding the real blocker behind a stale one costs more than the cheap check would have | [reference/peer-agent-blocker-needs-verification.md](reference/peer-agent-blocker-needs-verification.md) |
| An oversized project `CLAUDE.md`: why it gets split (rule vs story) into a `cp`-verified decision log instead of summarized, deriving the index instead of typing it, leaving headroom instead of grazing the limit, and grepping the codebase for dead pointers before moving a section | [reference/oversized-claude-md-split-into-decision-log.md](reference/oversized-claude-md-split-into-decision-log.md) |
| A screenshot tool painting a lazy-loaded `next/image` cell pure black while every neighbor renders: why cold cache is the wrong hypothesis, the `img.complete`/`naturalWidth` control that proves the site is fine, and a zsh word-splitting bug that manufactured a second, unrelated phantom 404 in the same investigation | [reference/pixelshot-black-box-on-lazy-image.md](reference/pixelshot-black-box-on-lazy-image.md) |

## Quick reference

- **Components:** PascalCase, prefixed by type — `ButtonAccount.jsx`, `CardAnalyticsMain.jsx`. Other files: kebab-case. Dirs: kebab-case.
- **Inside a component file:** exported component → subcomponents → helpers → static content.
- **Pure functions:** `function` keyword; concise conditionals without unnecessary braces.
- **Client code:** `'use client'` only for Web API access in small components — never for data fetching or state.
- **Text in JSX:** type real accented chars (á, é, í, ñ, ¿, ¡) directly — a backslash-u unicode escape renders as its literal characters between tags, not as the letter.
- **Builds:** never build locally — let Vercel build; read logs with `vercel inspect`.
- **Mobile tab rows:** una fila de tabs/pills que no cabe → una sola línea con scroll horizontal (`flex-nowrap overflow-x-auto min-w-0` + `shrink-0 whitespace-nowrap` en items), **nunca** `flex-wrap` apilado.

## Common mistakes

- Designing a page from memory with the design skills installed but never invoked → the output is indistinguishable from having no skills at all (generic look, forbidden AI tells, improvised palette). Walk the [Design stack](#design-stack--the-order-of-work-for-anything-visual) phase by phase and invoke each skill before that phase's work.
- Reaching for `'use client'` + `useState`/`useEffect` to fetch data → use an RSC + server fetch instead.
- Patching a leaf component to fix empty space or cramping → the cap is in an ancestor (page → layout → root → global CSS). Trace upward first.
- Sizing a flex row with `width:fit-content` (+ `max-width:100%`) so it "fills when there are many children" → with `min-width:0` children it collapses to about half the available width, and without the `max-width` it overflows and hides children behind a horizontal scrollbar. Pass the child count as a custom property and use `width: min(100%, calc(...))`. Verify by screenshotting at the target viewport width, not by reading the CSS. See [reference/layout.md](reference/layout.md).
- Committing `public/` while serving media from an external CDN → double-hosting. `public/` goes in `.gitignore` from the first scaffold.
- Leaving `next/image` on default optimization in front of an external CDN → it silently re-hosts every image on Vercel's own domain, which fails asymmetrically: cached (old) images keep working, freshly uploaded ones 402. `grep -c "_next/image"` every deployed surface and set `images.unoptimized: true` when the CDN already serves the final bytes. See [reference/deploy.md](reference/deploy.md).
- Using a backslash-u unicode escape in JSX text expecting an accented letter → it renders the raw escape characters; type the real char.
- Apilar tabs/pills con `flex-wrap` en móvil cuando no caben → usa una sola línea con scroll horizontal (`overflow-x-auto` + `min-w-0`). Ver [reference/mobile-design.md](reference/mobile-design.md).
- Adding a third-party pixel/analytics script to a site with a strict CSP without allow-listing its CDN → the loader is blocked *silently* (inline stub queues events that never send). Allow-list `script-src`/`connect-src` and verify the event reaches the vendor dashboard. See [reference/third-party-scripts-csp.md](reference/third-party-scripts-csp.md).
- Mounting `<Analytics />` and calling analytics done → the per-project switch is **off by default** and the component does not flip it; the site records 0 visitors and that history is unrecoverable. Enable it in the same step that takes the site to production. See [reference/vercel-web-analytics.md](reference/vercel-web-analytics.md).
- Ending every `<title>` in the brand's *abbreviation* and writing a purely descriptive `<h1>` → the full brand string then exists in no `<title>` and no `<h1>` on the site, and the short brand search is lost while the long one still works. Keep a `titleBrand` constant separate from `shortName`. See [reference/brand-in-title.md](reference/brand-in-title.md).
- Shipping a local-business site and calling search done at the sitemap → the Google Business Profile listing sits above the organic results, and a listing with **no declared hours** is dropped by the "Open now" filter entirely (reviews are not the gate; hours behave like it). Audit hours, pin, phone, website field and photos. See [reference/google-business-profile.md](reference/google-business-profile.md).
- Placing a page watermark behind the content with `z-index:-1` → every section with an opaque `bg-*` covers it and it looks intermittent. Use one fixed `mix-blend-multiply` layer above the content, and measure its opacity by subtracting two screenshots rather than eyeballing it. See [reference/page-watermark.md](reference/page-watermark.md).
- Rebuilding navigation by hand because a card in a draggable strip "doesn't respond" → the container took `setPointerCapture` on `pointerdown`, which retargets the `click` away from the child `<a>`. Capture only past the drag threshold and the anchor keeps its native click (plus ctrl/middle-click to a new tab). Test with a REAL click — `el.click()` passes with the bug in place. See [reference/draggable-card-strip.md](reference/draggable-card-strip.md).
- Narrowing one field in a row by adding `w-28` next to a shared class constant that already carries `w-full` → both classes land in the attribute and the **generated stylesheet's order** decides, not yours: `w-full` wins, the control eats the row and its neighbour is pushed off screen with no scrollbar and no error (measured: `right: 1050` against `innerWidth: 960`). Put layout overrides on a wrapping container, never a per-instance utility from the same family as the constant. See [reference/shared-class-constant-vs-per-instance-utility.md](reference/shared-class-constant-vs-per-instance-utility.md).
- Writing an opacity modifier that is not a multiple of 5 (`bg-brand/12`) → Tailwind 3's default scale steps by 5, so the class is **never generated**: the attribute still carries it, nothing warns, and the element paints fully transparent (measured: "invisible" avatar circles whose letter still showed). Stay on the scale or use the bracket form (`bg-brand/[12%]`), and confirm the color is declared with `<alpha-value>`. Diagnose by reading the computed color off the live element, with a working sibling class as the positive control. See [reference/tailwind-opacity-modifier-scale.md](reference/tailwind-opacity-modifier-scale.md).
- Writing a component class in `globals.css` and *also* passing Tailwind color utilities from the JSX → the unlayered class beats the whole `@layer utilities`, so some properties apply and others don't; the typical result is the active state rendering unreadable. One source of truth: the custom class owns every visual state. See [reference/css-cascade-layers-vs-tailwind.md](reference/css-cascade-layers-vs-tailwind.md).
- Pasting a product cut-out onto a generated interior, or generating the interior *with* the product in it → the model redraws the merchandise (a product the client does not sell), and even with the geometry perfect the mismatch in **light** between a warehouse photo and the generated room reads as "pasted" with no prompt fix. Generate the room EMPTY around the product's own photo, and don't composite non-studio-lit photos at all. See [reference/product-photo-compositing.md](reference/product-photo-compositing.md).
- Judging a vectorized line-art logo by `difference / ink pixels` → a perfect trace already spends ~4% on antialias halo, so every candidate looks equally bad. Measure difference *outside* a 1 px contour ring, and count solid cores. Validate the referee against a deliberately broken SVG before trusting it. See [reference/svg-line-art-tracing.md](reference/svg-line-art-tracing.md).
- Dropping favicons straight into a client-logo row, or unifying them with a monochrome treatment → the ones with a color plate baked in read as blocks next to floating drawings, and monochrome turns them into solid discs. Split on corner alpha and normalize both cases into the same round badge. See [reference/client-logo-row.md](reference/client-logo-row.md).
- Omitting an identity field (`author`, `owner`) from a POST because you assume the backend derives it from the token → if it validates the field instead, 100% of requests 400. Sending it costs nothing and works in both worlds. And never hand-type or hardcode a name you compare by equality — accented values have two byte encodings (NFC/NFD) that look identical on screen; pass the session value verbatim. See [reference/api-boundary-contracts.md](reference/api-boundary-contracts.md).
- Deciding in the browser that a server-issued deadline has passed (`data.open && Date.now() < expiresAt`) → the verdict is handed to the least trustworthy clock, and a device running fast locks the control on **every** record at once with no error and no log ("the system is down"). Let the clock only raise a `due` intention that triggers ONE guarded refetch, never let it reopen what the server closed, and assert the skew cases. See [reference/client-clock-vs-server-deadline.md](reference/client-clock-vs-server-deadline.md).
- Passing a Postgres `date` value straight into `new Date()` and formatting it with a `timeZone` → it arrives as midnight UTC and every row renders one day early, with no error anywhere. Slice to 10 chars and anchor to noon UTC, and keep calendar days and instants in separate, differently-named functions. See [reference/calendar-days-vs-instants.md](reference/calendar-days-vs-instants.md).
- Ingesting the "for humans" export of an external system (separate `Fecha` / `Hora inicio` columns) instead of its ISO-with-offset export → a row whose offset was already wrong becomes **permanently** wrong, because the file kept the broken rendering and dropped the zone that proved it (measured: +13 h on a real appointment). Prefer the offset-bearing export, let it win any merge, compute the expected offset for that date, and crash on an unrecognized schema. See [reference/calendar-days-vs-instants.md](reference/calendar-days-vs-instants.md).
- Keeping a UI verdict on a backend boolean after that field changed *meaning* (same name, same type, new truth) → nothing fails and the screen inverts: under the new semantics a healthy row is born `false`, so refetching right after a successful write flashes the failure state on **every** operation. Read the verdict from a status field, draw nothing when the field is `""` (that is absence of instrument, not a state), re-key the cache signature, and route errors on a body `code` rather than on the HTTP status. See [reference/api-boundary-contracts.md](reference/api-boundary-contracts.md).
- Writing a TypeScript type from the written API contract instead of from a measured response → a field the endpoint never returns becomes a compiler-endorsed `undefined` at runtime, which is worse than a crash. Measure first. See [reference/api-boundary-contracts.md](reference/api-boundary-contracts.md).
- Totalling money in JavaScript floats, or collapsing "what customers should pay" and "what actually came in" into one figure → the header stops matching the table underneath it and the panel looks healthier than the business is. Sum in integer cents, send amounts as strings, and keep committed and collected as two numbers. See [reference/money-dashboard.md](reference/money-dashboard.md).
- Treating an explanatory caption on a money screen as decoration → the grouping code can be right while the note under it says something false, and no typecheck, linter, or unit test catches it. Read every caption as an assertion about the code in front of you. See [reference/money-dashboard.md](reference/money-dashboard.md).
- Citing "the formula matches N historical rows" as proof without counting distinct input values → N rows can be a handful of distinct values repeated many times (measured: 157 "cases" were 6 distinct numbers), and a fixture your own code generated makes a perfect match circular rather than confirming. Count `new Set(rows.map(...)).size` before citing any count, and ask who wrote the file you're measuring against. See [reference/empirical-rule-verification-traps.md](reference/empirical-rule-verification-traps.md).
- Quoting `sum(fees) / sum(prices)` across a handful of rows as "the rate" → the number is correct and the label can still be false; a weighted average prints identically to a measured rate even when zero individual rows show that value and the real spread crosses an order of magnitude (measured: 1.8%–8.4%, reported as one 4.31%). Check whether any single observation equals the average before calling it a rule, and state the exact set any count was taken over — a metric that measures one table is not evidence about the whole system. See [reference/average-of-rates-is-not-a-rate.md](reference/average-of-rates-is-not-a-rate.md).
- Relaying a peer agent's stated blocker to the operator as-is → it reads like new information (it carries context, urgency, a peer's own measurement) which is exactly why the step of checking it against your own state gets skipped, and asking for something already given erodes trust in every ask after it. Check the one fact the blocker depends on yourself before escalating — usually you can. See [reference/peer-agent-blocker-needs-verification.md](reference/peer-agent-blocker-needs-verification.md).
- Summarizing an oversized project `CLAUDE.md` by hand instead of splitting it → a summary keeps conclusions and throws away the measurement/falsified-alternative/root-cause behind each one, so a decision gets reverted later because the reasoning is gone. `cp` (hash-verified) into a decision log, rewrite `CLAUDE.md` to one rule-line per decision, and write the "story is appended, only the rule stays" maintenance sentence INTO the file or it regrows to the same size. See [reference/oversized-claude-md-split-into-decision-log.md](reference/oversized-claude-md-split-into-decision-log.md).
- Reading a pure-black cell from a headless screenshot tool as "the image failed to load" → a `next/image` lazy variant can be `200` and fully decoded (`img.complete`, real `naturalWidth`) while the capture tool still paints it black, so cold cache is the wrong hypothesis and re-warming every variant does not fix it. Ask the live DOM for the `<img>` element's state before touching the site; treat the capture tool as the suspect when the screen and the tool disagree. See [reference/pixelshot-black-box-on-lazy-image.md](reference/pixelshot-black-box-on-lazy-image.md).
- Writing a template like `"{name} added to the order"` for a name an admin typed in a panel → the fixed number/gender agrees with some names and reads wrong with others, and the person naming the product has no reason to know they're writing a sentence's subject. Rewrite the sentence to not depend on the name (impersonal/present tense, counts in a badge) instead of guessing grammar. Also order a truncated label's distinguishing word FIRST — `truncate` keeps the start, not the meaningful part. See [reference/admin-entered-names-in-ui.md](reference/admin-entered-names-in-ui.md).
- Carving an exception into a rule for "the one vendor/case that doesn't fit," without counting how often that case occurs → if the exception covers the most frequent case in the data rather than a rare corner, the data is saying the rule is wrong, not that the case is special — and a string of individually-correct fixes on top of it still produces a mechanism that shouldn't exist. Retire it by deleting the computation, not just the rendering, and leave a tombstone comment naming the false premise. See [reference/money-dashboard.md](reference/money-dashboard.md).
- Shipping a detector's full classification (which owner, which periods) instead of just its verdict, on a dashboard multiple stakeholders view → every field can be individually correct and still, read together, publish a private fact to whoever opens the shared screen. Ship the signal ("this total is missing N periods"), never the evidence, and re-check every downstream sentence once a list-shaped field can shrink to 0 or 1. See [reference/money-dashboard.md](reference/money-dashboard.md).
- Adding a `no-store` / `revalidate: 0` fetch to `app/layout.jsx` (or to the header/footer it renders) → the layout wraps every page, so **every** route flips from static `○` to dynamic `ƒ`, and a slow API now slows down screens that never used that data. Give that read a `revalidate` matching how often the data really changes, and diff the build's route table before/after. See [reference/static-vs-dynamic-rendering.md](reference/static-vs-dynamic-rendering.md).
- Deduplicating or counting customers by comparing whole phone strings, or normalizing them only at display time → the same subscriber arrives with different country prefixes (some of them corrupt) and every aggregate is quietly wrong. Repair the prefix from the area code, compare by the last 10 digits, and normalize to E.164 at ingest. And do not accept two independent counts that agree as verification — errors of opposite sign cancel. See [reference/phone-normalization-and-dedup.md](reference/phone-normalization-and-dedup.md).
- Confirming DKIM by grepping the mail log for a success string → the happy path is **silent by design**, so the grep returns empty for healthy domains too and the documented check is a guaranteed false negative — whose natural next step is regenerating a key that was fine. Verify with an external receiver, and before trusting any grep as proof, run it against a case you know is healthy. See [reference/domain-email-auth.md](reference/domain-email-auth.md).
- Reporting a lint count on a repo that already had findings, or taking the baseline by linting a copy of the file under `/tmp` → a flat config scopes its rules by globs relative to the config, so the `/tmp` copy is linted with *fewer* rules and the diff invents a regression (measured: 0E for the baseline vs 5E live, actual delta zero). Take the baseline from a `git worktree` at the same relative path, with the same `node_modules` and the same command, and compare the reported **lines**, not the totals. See [reference/lint-delta-baseline.md](reference/lint-delta-baseline.md).
- Debugging "the whole page is shifted / deformed" as a layout bug → one flex row that cannot shrink overflows, and with the default `overflow: visible` that overflow lands in the **document's** scroll width. A screenshot at offset 0 looks fine in both versions; measure `documentElement.scrollWidth > clientWidth` headless instead, then contain the row in its own `overflow-x-auto` scroller with `shrink-0` items. See [reference/overflowing-row-scroll-container.md](reference/overflowing-row-scroll-container.md).
- Styling a disabled control with a *surface* token (`disabled:bg-<surface-sunken>`) → on the screen that uses that surface as its **page** background the control renders at 1.00:1 and disappears, leaving its label reading as stray body copy ("why is that sentence floating there?"). Give the disabled state its own token, off every page surface, and pair it with a body-text label token. See [reference/surface-tokens-and-disabled-state.md](reference/surface-tokens-and-disabled-state.md).
- Fixing a "broken rounded corner" focus ring by giving the inner button the parent's radius → a plain `outline` paints *outside* the border box, and since the child fills the parent exactly, `overflow-hidden` clips the whole ring away: the control ends up with **no** focus indicator, which is an a11y regression disguised as a fix. Hang the ring on the rounded container with `has-[:focus-visible]:outline-*`. See [reference/focus-ring-clipped-by-rounded-parent.md](reference/focus-ring-clipped-by-rounded-parent.md).
- Pushing edits from a checkout with no `node_modules` and letting the remote build be the first thing that can say no → `node --check` cannot parse JSX, so a comment closed early or an unclosed element ships. `new Bun.Transpiler({ loader: 'jsx' })` parses without resolving imports, needs nothing installed, and catches exactly that class. Prove the gate fails on a broken file before trusting it. See [reference/syntax-gate-without-node-modules.md](reference/syntax-gate-without-node-modules.md).
- Trusting `.gitignore` to keep a file out of a deployment → `vercel deploy` uploads the **working directory**, so a gitignored route serves publicly (200) while `git status` stays clean and `git ls-files` never lists it. Only `.vercelignore` excludes it — and while the files remain on disk, deploying **re-publishes** them. See [reference/deploy.md](reference/deploy.md).
- Modelling an optional price or duration as a PocketBase `number` → the field is `NOT NULL DEFAULT 0`, so "never captured" reaches the page as `0`: the card renders **"$0"** and a zero-length duration collides with nothing, letting a scheduler stack bookings. Use a `select` when the values are discrete, or keep the number and enforce "a *visible* record has a price" in a `create` **and** `update` hook. See [reference/pocketbase-schema-and-rules.md](reference/pocketbase-schema-and-rules.md).
- Reading an empty PocketBase collection as proof that its access rule is locked → a denied `list` returns `200` with `items: []` and a denied `view` returns `404`, so empty and gated look identical. Seed a record and confirm superuser sees 1 while anonymous sees 0 — and read the denial signal per collection, since a rules-NULL collection answers `403` where another answers `404`. See [reference/pocketbase-schema-and-rules.md](reference/pocketbase-schema-and-rules.md).
- Locating a public form's own row with `?filter=key="<secret>"` → that read needs `list` open, which a public form must not have, so the id is never obtained and the failure surfaces only on the **second** visit: the retry `POST` hits the unique index and the visitor can answer once but never correct themselves. Burn a deterministic 15-character `id` into the client, `PATCH` it, and fall back to `POST` with that same id on `404`. See [reference/pocketbase-public-editable-form.md](reference/pocketbase-public-editable-form.md).
- Writing a failure message for the team and shipping it to the screen → nothing rejects it, and the reader gets a developer's name to notify, a column name (`visible=false`), or an instruction addressed to a role they themselves are (measured: a guard telling the client to wait for "the client" to confirm). Sweep user-visible strings separately from log strings, remember that staff-only is client-facing when the owner has a staff account, and if the copy promises an automatic retry, implement it. See [reference/user-facing-error-copy.md](reference/user-facing-error-copy.md).
- Deploying the front ahead of its backend and calling it safe because nothing crashes → the new control still renders, the request still returns **200**, and the value the author chose is silently replaced by the old default: the only field that fails is the only one they cared about. Gate every new control on a flag derived from the *response* (`"key" in body`, checked before the response is flattened — never `.length`), keep the new fields out of the request when it is false, and say on screen what is missing. See [reference/capability-gate-front-ahead-of-backend.md](reference/capability-gate-front-ahead-of-backend.md).
- Collapsing a not-yet-deployed route into `if (!res.ok) return null` → the screen tells the user their **session expired**, so they sign out, retype their password, sign in fine and hit the same message in a loop. Split `401` (no-session, fixable by signing in) from any other failure (no-backend, not fixable from the browser). See [reference/capability-gate-front-ahead-of-backend.md](reference/capability-gate-front-ahead-of-backend.md).
- Attaching the domain without shipping `app/robots.js` + `app/sitemap.js` and handing the operator the Search Console steps → the site is live and invisible. Also: dropping a development `noindex` without setting `alternates: { canonical: '/' }` leaves the `*.vercel.app` preview competing with the real domain. See [reference/indexing-and-search-console.md](reference/indexing-and-search-console.md).
- Typing the sitemap's URLs by hand, or padding it with a build-time `lastModified` plus `priority`/`changeFrequency` → the list starts lying at the first record created from the panel, and dates that all move together on every deploy are the pattern crawlers learn to ignore. Derive it from the same array the routes use and ship bare `<loc>` entries. See [reference/indexing-and-search-console.md](reference/indexing-and-search-console.md).
- Validating a scroll-reveal animation with a `fullPage` screenshot → the capture resizes the viewport to the whole document, so every element enters view, the `view()` timeline resolves to 1, and an element permanently stuck at `opacity: 0` photographs as visible. Read the computed opacity in a real viewport without scrolling, and confirm `CSS.supports('animation-timeline','view()')` first or the empty result means nothing. See [reference/scroll-reveal-stuck-at-opacity-zero.md](reference/scroll-reveal-stuck-at-opacity-zero.md).
- Counting a rendered string with `grep -c`, or comparing a `>text<` count against a bare-string count → the RSC flight payload carries a second copy of every visible string, so the bare form doubles while the anchored form does not, and a healthy page fails its own check. Anchor **both** sides to the markup, count with `grep -o … | wc -l`, and run a positive control before believing any zero. Same family: `[^<]*` to read an interpolated value stops dead at React's `<!-- -->` boundary separator and reports a blank — use a raw character window (`.{0,120}` / `repr()`) instead. See [reference/counting-strings-in-rendered-html.md](reference/counting-strings-in-rendered-html.md).
- Confirming a deploy by polling a URL until it responds, or by grepping for a utility class/string the project already used before the change → both read as "verified" while serving the OLD bundle. Pick a witness unique to the diff, in pure ASCII (accented text is escaped in minified bundles and produces a guaranteed false negative), and run a positive control before trusting a zero. See [reference/deploy-verification-witness-string.md](reference/deploy-verification-witness-string.md).
- Creating a new module and considering it done because it's well written and compiles → only the importer distinguishes "done" from "written"; a pricing-rule module sat unimported for a week while the site kept showing the old price. Grep for `from.*<module-name>` with a positive control before closing any new module. See [reference/unwired-module-created-not-imported.md](reference/unwired-module-created-not-imported.md).
- Fixing only the READ side of a capture screen (the list/detail view) and leaving the WRITE side's duplicated validation untouched → the screen shows the object, takes the input, lights up Save, and answers "that object doesn't exist," which reads as success to the person typing. Verify by actually saving a real value, confirming it in the store, refreshing, and checking the counter moved by exactly one — never by reading the diff. See [reference/capture-half-fix-reads-not-writes.md](reference/capture-half-fix-reads-not-writes.md).
- Inspecting an API response with `echo "$R" | jq` in a zsh shell → `echo` expands the `\n` escapes *inside* the JSON into real newlines, so `jq` reports a parse error on a document that arrived perfectly well, and in a script the emptied variable builds a truncated URL whose `404` reads as "not found" or "no permission". Use `printf '%s' "$R" | jq`, `jq . <<<"$R"`, or `curl -o /tmp/r.json` + `jq . /tmp/r.json` in scripts. See [reference/shell-echo-corrupts-json.md](reference/shell-echo-corrupts-json.md).
- Branching a client error handler on `frame.code` alone while the server also sent `message` → three different reasons for the same rejection (bad recipient, empty body, body too long) render as one generic toast, and the user cannot find out why their action fails even though the server already said so on the wire. The `code` picks the family, the `message` picks the phrase (mapped to i18n, never rendered raw). See [reference/user-facing-error-copy.md](reference/user-facing-error-copy.md).
- Showing a character counter with `draft.length` against a server that measures `[...body].length` → UTF-16 units, code points and UTF-8 bytes give 10 000 / 5 000 / 20 000 for the same 5 000 emoji, so an ASCII-only test passes with the counter wrong. Count code points, replay the server's whole pipeline (`trim` → NFC → count), deploy the server before the client that promises its ceiling, and pin the constant in a test instead of retyping the number. See [reference/client-mirrored-server-limit.md](reference/client-mirrored-server-limit.md).
- Reusing the chart palette to color small text (usernames, tags, labels) because the tokens already say "validated" → the floor for a mark is 3.0 and for small text 4.5, so validated-for-charts colors fail as text (measured: 4 of 6), and measuring against the *page* background instead of the darkest surface the color can land on makes the same label pass on one screen and fail on another. Derive a text variant by multiplying the channel (subtracting desaturates and kills the hue), and store the slug, not the hex. See [reference/categorical-palette-as-text-color.md](reference/categorical-palette-as-text-color.md).
- Answering `422`/error from a route that assigns something already-existing (a retried tag, a re-attached file, a repeated group membership) → whoever retried already got the state they asked for, and forcing the client to tell "failed" from "already there" apart gets built wrong exactly once, silently. Answer `200`/`201` with an explicit `already_had_it` field (never discriminate by the HTTP status — proxies flatten 200 vs 201), and key the assignment by its own relationship row id, not by the file/entity's uuid. See [reference/idempotent-assignment-endpoint.md](reference/idempotent-assignment-endpoint.md).
- Working a bug from its saved root cause without re-measuring first, in a repo touched by more than one session/agent → the world has usually moved since the bug was filed (the blocker it names can already be fixed, and the real blocker it never saw goes untouched). Treat a blockers file as dated observations, not facts, and re-run the measurement before working anything more than a day old. See [reference/stale-bug-diagnosis.md](reference/stale-bug-diagnosis.md).
- Running a test suite against a sync endpoint whose contract is REPLACE (delete + reinsert), even against production → exercising the endpoint exactly as documented empties the table, needs no bug, and can self-heal invisibly if something else re-syncs shortly after — until the one time nobody does. Give any suite that can call a destructive endpoint its own database, not just namespaced row cleanup. See [reference/test-suite-destructive-endpoint.md](reference/test-suite-destructive-endpoint.md).
- Hardcoding a test fixture constant chosen for a property (doesn't divide evenly, falls in a range) instead of deriving it from the rule → the day a business rule changes around it (a new cap, a new boundary), the constant can become illegal input or stop exercising the edge it was built for, and the gate fails on its own fixture or passes without testing anything. Derive the value from the rule and assert the fixture's own premise. See [reference/stale-test-fixture-constant.md](reference/stale-test-fixture-constant.md).
- Bulk-updating a "set of labels" field (`tags`, a multi-select) with `PUT`/`PATCH` and only the new value → many such APIs REPLACE the whole array rather than append, so the other tags on that row (payment method, category) vanish silently with a `200`. Canary one row, read it back through the API, and only then run the batch. See [reference/collection-field-replace-vs-append.md](reference/collection-field-replace-vs-append.md).
- Trusting "the backend already dedups/validates this" from a doc without opening the route you actually call → the guarantee usually lives in ONE writer's code (a hand-written script, an admin panel), not in the table, and a second route (a new API endpoint) can be a bare write with no net at all. Two writers share only what the resource itself enforces (`UNIQUE`, a trigger, a `CHECK`) — never what one writer's code enforces. Name the writer explicitly when documenting a guarantee. See [reference/guarantee-scoped-to-the-writer.md](reference/guarantee-scoped-to-the-writer.md).
- Moving who *sees* a screen without moving who *holds* the permission — or checking it with an assertion that the role has the capability → the two halves stay individually correct while the act becomes reachable by nobody, and the check goes green because it measures the table you already wrote. Assert reachability instead (someone sees it AND can execute; no dead screens; no unusable permissions; every **grantable** capability appears in code that requires it), and name which layer a cross-layer debt is missing. See [reference/permission-without-a-screen.md](reference/permission-without-a-screen.md).
- Unblocking or fixing a gated flow and reporting it done from the code alone → the code can be entirely correct against a wrong model of who does the work (measured: the capability sat with 1 of 17 people, and the person who actually does that job wasn't one of them). Always ask "who exactly can reach this?", enumerate them, and verify by logging in as that person — not by reading the permissions table. If someone is already doing the work without the permission, that's a contradiction pointing at another door the flow should be using. See [reference/permission-without-a-screen.md](reference/permission-without-a-screen.md).
- Using `robots.txt` `Disallow` to keep a page out of the results, or shipping `metadataBase` and assuming pages now declare a canonical → `Disallow` blocks crawling, not indexing (a linked URL still appears), and `metadataBase` alone emits **no** canonical element. Use per-page `robots: { index: false }`, add `alternates: { canonical: './' }`, and check that both the apex and the `www` don't return 200 independently. See [reference/indexing-and-search-console.md](reference/indexing-and-search-console.md).
- Reporting a count from a single paginated API request (`head -12`, `limit=100` on Graph) as the total → the endpoint returns exactly what you asked for and never says there's more, so a request for 100 that returns 100 reads as a real, plausible total (measured: reported 117 comments, actual 269). Check whether the count equals the limit you asked for, rerun with a different limit as a positive control, and follow `paging.next` as a complete URL rather than reconstructing it. See [reference/paginated-count-truncated-silently.md](reference/paginated-count-truncated-silently.md).
- Leaving a lookup against the OLD catalogue/enum in place after a vocabulary migration → the old code doesn't error, it just resolves against a dead universe, and a counter built on it can report a large, plausible number that is 100% retired-catalogue debt while the live universe reads zero (measured: 194 "to ask" vs a true 889). Classify every read/count/write against the old collection, bridge through an attribute both universes share, and seed one live record to prove the new count actually moves. See [reference/retired-vocabulary-vs-live-universe.md](reference/retired-vocabulary-vs-live-universe.md).
